node_modules
dist
dev-dist
.DS_Store
*.local

.env
.env.local

# the Strava tokens — a credential, never committed
.strava.json

# les photos reçues de l'app — des données d'athlète, pas du code
var/
