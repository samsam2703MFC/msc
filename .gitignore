node_modules
dist
dev-dist
.DS_Store
*.local

.env
.env.local

# l'ancien magasin de jetons Strava, d'avant la base — au cas où il traîne
.strava.json

# les photos reçues de l'app — des données d'athlète, pas du code
var/

# le classeur empaqueté par le runner, envoyé au serveur
outils/
